services:
  fastapi:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./models:/app/models
    restart: unless-stopped
    networks:
      - wooriport-network

networks:
  wooriport-network:
    external: true
