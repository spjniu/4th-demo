# vite build (npm run build) 일 때 자동 적용 — 배포용
#
# 단일 CloudFront 구성: 프론트(S3)와 API(ALB)가 같은 origin(*.cloudfront.net)이라
# 상대경로를 쓴다. 빈 값이면 코드에서 "/api/v1", "/propose" 처럼 상대요청 →
# CloudFront 가 경로별로 S3/ALB 로 보냄. (CORS·mixed-content 없음)
VITE_API_BASE=
VITE_AI_BASE=
