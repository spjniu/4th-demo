const BASE_URL = 'http://localhost:8000/agent';

export async function generateReport(
  userId: string | number,
  prevPortfolio: unknown,
  curPortfolio: unknown
) {
  // POST /report
  console.log('API 연동 전');
}
